services:
  app:
    build: .
    working_dir: /workspace
    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules
      - npm_cache:/root/.npm
    command: sh -c "npm ci && npm run check"

volumes:
  node_modules:
  npm_cache:
